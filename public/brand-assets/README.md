# Brand assets (design archive)

Canonical **design masters** for Inabiya (AI / PDF / high-res PNG).

Web runtime copies (clean URLs, cropped nav wordmarks, favicons) live in:

`apps/web/public/brand/`

Use `BrandLogo` / `lib/brand-assets.ts` in the web app — do not link directly to this folder from Next.js (spaces in paths + not under `apps/web/public`).
