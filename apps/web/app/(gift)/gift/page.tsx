export default function GiftHomePage() {
  return (
    <main className="min-h-screen p-8 bg-[var(--background)] text-[var(--foreground)]">
      <p className="font-body text-sm uppercase tracking-widest text-[var(--primary)]">Inabiya</p>
      <h1 className="font-display text-4xl mt-2">Gift</h1>
      <p className="mt-2 max-w-prose opacity-80">
        Soft Gift storefront shell (System A). Catalog and builder arrive in later phases.
      </p>
    </main>
  );
}
